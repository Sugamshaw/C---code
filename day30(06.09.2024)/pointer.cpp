#include <iostream>
#include <vector>
#include <algorithm>
#include <unordered_map>
using namespace std;
int **func(*float a,char **c)
{
}
int main()
{
    int a = 100, b = 2000, c = 15;
    int *arr[] = {&a, &b, &c};
    cout << arr[1] << endl;
    // int *p = &a;
    // int *q = &b;
    // p = q;
    // cout << p << endl;
    // cout << *p << endl;
    // cout << q << endl;
    return 0;
}